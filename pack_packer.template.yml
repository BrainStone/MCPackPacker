# Define a template job
.pack_pack:
  script:
    - |
      @pack_packer.sh@
  artifacts:
    paths:
      - '*.zip'
  cache:
    - key: "CompressionCache"
      paths:
        - .cache
      policy: pull-push
