# Define a template job
.pack_pack:
  script:
    - |
      #!/usr/bin/env bash
      
      # Dummy file for now
      echo "Hello World!"
  artifacts:
    paths:
      - '*.zip'
  cache:
    - key: "CompressionCache"
      paths:
        - .cache
      policy: pull-push
